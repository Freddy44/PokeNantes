{% extends 'NosBundlesProductBundle::layout.html.twig' %}

{# -- title -- #}
{% block title %}Liste Catégorie{% endblock %}

{# -- titre page -- #}
{% block title_page %}<h1>Nom catégorie</h1>{% endblock %}

{# -- contenu page -- #}
{% block main %}
	{% for product in products %}
				<article>
					<img src="{{ product.prodPicture }}" alt="{{ product.prodPicture }}">
						<table border="1">
							<tr>
								 <th>
									 <h3>
											<a href="{{ path('product_edit', { 'id': product.prodId }) }}">
												{{ product.prodName }}
											</a>
										</h3>
									</th>
								 <td>
								 	<button>
										<a href="{{ path('product_show', { 'id': product.prodId }) }}">
											Modifier fiche produit
										</a>
									</button>
									<button>
										<a href="{{ path('product_edit', { 'id': product.prodId }) }}">
									</button>
								 </td>
							</tr>
							<tbody>
									<tr>
											<td><a href="{{ path('product_show', { 'id': product.prodId }) }}">{{ product.prodRef }}</a></td>
											<td>{{ product.prodCat }}</td>
											<td>{{ product.prodDesc }}</td>
											<td>{{ product.prodState }}</td>
											<td>{{ product.prodQty }}</td>
											<td>{{ product.prodQtyDefect }}</td>
											<td>{{ product.prodId }}</td>
											<td><div class="tags-list">{% for Provider in product.ProvidersList %}
											{% if loop.index > 1 %}
											{% endif %} <span class="badge">
															{{ Provider.provName }}
															{{ Provider.provType }}
															{{ Provider.provPhone }}
															<br />
															</span>{% endfor %}
											</div>
										</td>
											<td>
													<ul>
															<li>
																	<button><a href="{{ path('product_show', { 'id': product.prodId }) }}">show</a><
															</li>
															<li>
																	<button><a href="{{ path('product_edit', { 'id': product.prodId }) }}">edit</a>
															</li>
													</ul>
											</td>
									</tr>
							</tbody>
						</table>
				</article>
		{% endfor %}
{% endblock %}
